export const createPotato = () => {
    // let citiesHTML = "<ul>"

    // for (const walker of walkers) {
    //     citiesHTML += `<li>${walker.city}</li>`
    // }

    // citiesHTML += "</ul>"

    // return citiesHTML
}